<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar"
        tools:targetApi="31">

        <!-- Your activities -->
        <activity android:name=".Activity.gihan" android:exported="false" />
        <activity android:name=".Activity.belt_screen" android:exported="false" />
        <activity android:name=".Activity.cotton_short" android:exported="false" />
        <activity android:name=".Activity.profile_screen" android:exported="false" />
        <activity android:name=".Activity.overt_two" android:exported="true" /> <!-- Needed since wishActivity2 navigates to it -->
        <activity android:name=".Activity.wishActivity2" android:exported="true" /> <!-- This should be accessible -->
        <activity android:name=".Activity.discover_page" android:exported="true" />
        <activity android:name=".Activity.home_Activity" android:exported="true" /> <!-- Make sure home screen is accessible -->
        <activity android:name=".Activity.create_Activity2" android:exported="false" />
        <activity android:name=".Activity.IntroActivity2" android:exported="false" />

        <activity
            android:name=".Activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
